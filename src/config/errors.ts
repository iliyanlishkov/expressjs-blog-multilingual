export const errorsDir = "storage/logs/errors.log"; // the operational errors log directory
export const exceptionsDir = "storage/logs/exceptions.log"; // the unhandled exceptions log directory (programming error)
export const rejectionsDir = "storage/logs/rejections.log"; // the unhandled promise rejections log directory (programming error)
export const osSignalsDir = "storage/logs/ossignals.log"; // the os signals log directory (notice)
