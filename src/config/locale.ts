export const default_locale: string = 'en'; // the main language of the app
export const all_locale: string[] = ['en', 'bg']; // all languages of the app
export const fallback_locale: string = 'en'; // will be used when the active language does not contain a given translation string
export const translationsDir: string = './dist/translations/lang'; // this is the main translations directory from where the translations will be extracted