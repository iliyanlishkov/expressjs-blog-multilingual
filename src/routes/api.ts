import { Router } from 'express';
// import { handleRefreshToken } from "../controllers/Auth/RefreshTokenController.js";

const router = Router();

// router.get('/reissue-access-token', handleRefreshToken);

export default router;